name: label_scanner
description: Barcode-Etiketten scannen und den richtigen Code auswählen – Sendungsnummer oder Fahrgestellnummer.
version: 1.0.0

# Kein Paket für pub.dev: die Einbindung läuft über Git, siehe README.
publish_to: 'none'

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
